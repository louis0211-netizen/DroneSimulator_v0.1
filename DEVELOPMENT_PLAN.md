# DEVELOPMENT_PLAN

## Phase 1: Flight Physics Core

Goal: A quadcopter can arm, take off, hover, move, yaw, land, crash, and reset using Rigidbody-based physics.

Implemented first:

- `DronePhysics`
- `MotorMixer`
- `FlightController`
- `DroneInputManager`
- Shared flight data types
- Fallback compile-check harness

Validation:

- Compile scripts in Unity 6.
- Verify Rigidbody movement in Play Mode.
- Tune hover throttle and motor response before adding richer features.

Status:

- Runtime fallback C# compile check passed.
- Editor fallback C# compile check passed.
- Unity Hub and Unity Editor `6000.3.23f1` were installed.
- Unity batchmode compile/import passed after Unity login/license activation.
- MVP scene was generated by Unity batchmode.
- On this Windows machine, Unity CLI did not expose an installable `ios` module.

## Phase 2: Control Feel

- Add PID profiles for Stabilized mode.
- Add rate tuning for Acro mode.
- Add expo curves and dead zones for virtual sticks.
- Add preset-driven mass, arm length, drag, and motor values.

Status:

- First PID/rate profile is implemented in `DronePreset`.
- Stabilized mode uses angle-to-rate control, then rate PID to motor mixer demand.
- Acro mode uses rate PID directly.
- Mixer authority and armed idle output are preset-driven.
- Stick dead zone, stick expo, and throttle expo are preset-driven.

## Phase 3: iOS Input

- Build landscape UI canvas.
- Add left and right virtual joysticks.
- Add ARM / RESET / CAMERA / FLIGHT MODE buttons.
- Validate touch behavior on device through Xcode install.

Status:

- Landscape configurator is implemented.
- Editor scene builder creates a first-pass touch HUD and buttons.
- Safe-area fitting is implemented for iPhone landscape UI.
- Button labels now update at runtime for ARM/DISARM, camera, and flight mode.

## Phase 3.5: MVP App Loop

- Add a runtime training session layer outside flight physics.
- Guide the first flight loop:
  - ARM
  - Take off
  - Hover
  - Pitch forward
  - Pitch backward
  - Roll/strafe
  - Yaw
  - Land
  - Crash/reset recovery
- Show throttle lockout, crash, and low-battery warnings.
- Keep the app flow separate from `DronePhysics`, `FlightController`, and `MotorMixer`.

Status:

- `FlightTrainingSession` implemented.
- MVP scene builder wires training objectives, warning text, and dynamic button labels.
- Batch scene refresh entrypoint now rebuilds the MVP scene instead of only checking that it exists.

## Phase 6: Build Pipeline

- Configure iOS landscape build settings from an Editor menu.
- Generate MVP scene from an Editor menu.
- Provide a batchmode entrypoint for scene generation and iOS configuration.
- Provide an iOS Xcode project build entrypoint.
- Prepare Unity Build Automation iOS settings.

Status:

- `Drone Simulator > Configure iOS Landscape Build` implemented.
- `Drone Simulator > Create MVP Training Scene` implemented.
- `Drone Simulator > Build iOS Xcode Project` implemented.
- Bundle ID set to `com.louis0211netizen.dronesimulator`.
- MVP scene is added to Unity Build Settings automatically.
- Unity Build Automation setup guide added.

Platform note:

- iOS/Xcode export should be run on macOS with Xcode and iOS Build Support available.
- Windows can still be used for source development and non-iOS compile/import checks after Unity license activation.

## Phase 4: Camera and HUD

- FPV camera mounted to drone.
- Chase camera follow rig.
- HUD values:
  - Altitude
  - Speed
  - Flight Time
  - Flight Mode
  - Battery simulation

Status:

- FPV and chase cameras are implemented.
- HUD displays altitude, speed, flight time, flight mode, arm state, and battery.
- Objective/warning HUD is implemented through `FlightTrainingSession`.

## Phase 5: Training Ground

- Ground plane
- Takeoff pad
- Gates
- Obstacles
- Reset spawn point

Status:

- Runtime-generated training environments are implemented.
- City, Forest, Mountain, and Beach themes can be selected from the in-game HUD.
- Each theme rebuilds the training ground, updates atmosphere/fog/light color, and resets the drone/training loop.
- Visuals are still lightweight primitive geometry for fast iPhone testing; next pass should replace key props with optimized mobile meshes/materials.

## Later Architecture

- PID tuning UI
- Drone preset assets
- Wind volumes
- FPV racing checkpoints
- Physical controller input bridge
- Replay recorder
- Multiplayer synchronization layer
